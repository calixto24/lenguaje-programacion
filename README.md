activar entorno virtual

```bash
venv\Scripts\activate
```

instalar dependencias

```bash
pip install -r requirements.txt
```

ejecutes el servidor
```bash
uvicorn main:app --reload
```

depues de instalar una dependencia

```bash
pip freeze > requirements.txt
```
